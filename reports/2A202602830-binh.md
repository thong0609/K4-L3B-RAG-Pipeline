# Individual contribution report

## Thông tin

- **Họ và tên:** Đinh Văn Bình
- **Mã học viên:** 2A202602830
- **Nhóm:** Manngusidan
- **Chủ đề:** Tuyển sinh Đại học Quốc gia Hà Nội 2026.
- **Repository/branch:** [K4-L3B-RAG-Pipeline](https://github.com/thong0609/K4-L3B-RAG-Pipeline), branch `binh`.
- **Vai trò:** Retrieval — Task 5, 6, 7 theo [phân công nhóm](../docs/PHAN_CONG.md).

## Phần việc đã thực hiện

Bằng chứng triển khai chính: commit [`39493f5`](https://github.com/thong0609/K4-L3B-RAG-Pipeline/commit/39493f5).

| Module/deliverable | Việc tôi trực tiếp làm | File minh chứng | Trạng thái |
|---|---|---|---|
| Task 5 — Dense search | Dùng chung embedding với Task 4; truy vấn Chroma, đổi cosine distance thành score, giới hạn `top_k`, loại ID trùng và giữ metadata nguồn. | [task5_semantic_search.py](../src/task5_semantic_search.py) | Done |
| Task 6 — BM25 | Đọc đúng các chunk đã index; chuẩn hóa Unicode tiếng Việt; lọc chunk có token khớp; cache BM25 theo token corpus để cập nhật khi nội dung đổi. | [task6_lexical_search.py](../src/task6_lexical_search.py) | Done |
| Task 7 — RRF | Gộp thứ hạng dense/BM25 theo ID; xử lý ID trùng, đồng điểm và tham số biên; giữ nguyên score đầu vào. | [task7_reranking.py](../src/task7_reranking.py) | Done |
| Kiểm thử và hướng dẫn tích hợp | Bổ sung test offline và hướng dẫn sử dụng ba hàm trả về `list[SearchResult]` cho pipeline. | [test_retrieval.py](../tests/test_retrieval.py), [test_rrf.py](../tests/test_rrf.py), [test_contracts.py](../tests/test_contracts.py), [RETRIEVAL.md](../docs/RETRIEVAL.md) | Done |

## Quyết định kỹ thuật quan trọng

1. **Giữ cosine score gốc và fusion theo thứ hạng.** Dense trả `1 - distance`, kể cả điểm âm; RRF cộng `1 / (k + rank)` với `k=60` mặc định. Cách này tránh cộng trực tiếp hai thang điểm cosine/BM25 khác nhau và giữ score dense để Task 9 quyết định fallback. **Trade-off:** RRF không thể hiện độ tin cậy tuyệt đối, không dùng làm ngưỡng fallback; test xác nhận fusion không sửa score dense đầu vào.
2. **BM25 dùng cùng snapshot chunk với dense.** Đọc ID, nội dung và metadata từ Chroma; cache index theo token corpus thay vì số chunk. Test xác nhận thay nội dung dù giữ nguyên số chunk vẫn cập nhật index. **Trade-off:** mỗi query vẫn phải đọc và tokenize toàn corpus, phù hợp dữ liệu nhỏ nhưng cần tối ưu khi mở rộng.

## Kiểm thử và kết quả

Chạy lại ngày **25/09/2026**, bằng Python trong `.venv`:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_retrieval.py tests/test_rrf.py -q
# 29 passed
.\.venv\Scripts\python.exe -m pytest -q
# 49 passed
```

- **Trường hợp kiểm thử:** `HỌC PHÍ!` ở dạng Unicode NFD vẫn khớp nội dung tiếng Việt; `astronaut` không khớp corpus giả trả `[]`; query rỗng, corpus rỗng và `top_k <= 0` được xử lý đúng.
- **Kết quả tích hợp trên dữ liệu giả:** với query `tuition`, chunk liên quan đứng thứ hai ở dense được RRF đưa lên đầu khi kết hợp BM25; score dense ban đầu vẫn là `0.9`. Đây là kiểm tra hành vi, chưa chứng minh cải thiện trên corpus thực.
- **Các lỗi biên đã xử lý:** không loại nhầm chunk khớp từ khi BM25 có điểm bằng 0 hoặc âm; không dùng cache cũ khi corpus đổi; không cộng lặp ID trong một danh sách RRF. Lỗi provider được truyền lên caller để pipeline xử lý.
- **Phạm vi test tự động:** test retrieval dùng collection/embedding giả và BM25 thật, không gọi API. Các lệnh trên chạy bình thường sau khi sửa lỗi import Task 4, không thay module Task 4 bằng stub.
- **Sửa lỗi tích hợp sau merge:** khôi phục các hàm embedding của Task 4 bị mất để gỡ lỗi `NameError`; Task 5 truyền `is_query=True` và test xác nhận cờ này. Đây là sửa phụ thuộc để chạy retrieval, không phải nhận toàn bộ phần triển khai Task 4.
- **Dense search trên corpus thật:** cấu hình Gemini `gemini-embedding-001`, 768 chiều, collection 517 chunks. Query "Các phương thức xét tuyển năm 2026 của Trường Đại học Giáo dục?" trả top 3: `legal/thong-tin-tuyen-sinh-2026-dh-giao-duc-dhqghn.md::chunk-34` (0.7743), `news/article_04.md::chunk-0` (0.7701), `legal/thong-tin-tuyen-sinh-2026-dh-giao-duc-dhqghn.md::chunk-64` (0.7699). Kết quả xác nhận truy vấn API và index hoạt động, chưa chứng minh mức cải thiện của hybrid.
- **A/B:** báo cáo nhóm nằm tại [RESULT.md](../group_project/evaluation/RESULT.md). Tôi chưa trực tiếp xác minh các số liệu A/B bằng lần chạy evaluation đầy đủ, nên chưa kết luận mức cải thiện chất lượng end-to-end.

## Điều còn hạn chế

- Tokenizer BM25 chưa tách từ ghép tiếng Việt; việc đọc toàn corpus mỗi query còn tốn chi phí. Bonus reranker nâng cao chưa triển khai.
- Nếu có thêm thời gian, tôi sẽ phối hợp chạy A/B trên golden dataset để xác định lỗi retrieval thực tế, sau đó thử tokenizer tiếng Việt hoặc reranker và đo chất lượng cùng độ trễ so với RRF.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- **Ngày:** 25/09/2026
- **Tên thành viên:** Đinh Văn Bình
