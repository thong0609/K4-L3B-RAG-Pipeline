# Individual contribution report

---

## Thông tin

- **Họ và tên:** Tô Huy Thông
- **Mã học viên:** 2A202602608
- **Nhóm:** Manngusidan
- **Repository/branch:** `thong/ui-eval`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| **Giao diện Chatbot (UI)** | Xây dựng giao diện Streamlit có lịch sử chat, mở rộng (expander) để xem metadata và điểm score của các nguồn tìm kiếm. Tích hợp data giả lập để test. | `app.py` / commit `727692e` | Done |
| **Golden Dataset** | Đọc các văn bản Markdown (legal, news) để trích xuất 15 cặp Q&A kèm ngữ cảnh chuẩn xác. | `group_project/evaluation/golden_dataset.json` / commit `727692e` | Done |
| **Evaluation Script** | Viết file Python chuẩn bị khung sườn đo lường 4 metric của Ragas (Faithfulness, Relevancy, Precision, Recall) và A/B test (dense vs hybrid). | `group_project/evaluation/evaluate.py` / commit `727692e` | Partial (Chờ ráp pipeline) |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Sử dụng Kỹ thuật Mock Data để phát triển giao diện UI song song (Parallel Development).
   **Lý do/evidence:** Tránh tình trạng bị "chết thời gian" (block) khi chờ các thành viên khác hoàn thành module indexing, retrieval và generation.
   **Trade-off:** Mất thêm chút thời gian viết hàm giả lập (mock function), nhưng đẩy nhanh tiến độ làm UI lên rất nhiều.

2. **Quyết định:** Cấu trúc file `evaluate.py` sử dụng thư viện `ragas` và định dạng `Dataset` của HuggingFace ngay từ đầu.
   **Lý do/evidence:** Đảm bảo khi các module khác hoàn thành, chỉ cần thay một dòng code import là tự động chạy chấm điểm LLM-as-a-judge ngay mà không cần xử lý lại cấu trúc file.
   **Trade-off:** Ragas sử dụng LLM để chấm bài nên sẽ tốn token API (chi phí), nhưng bù lại kết quả đánh giá (4 metrics) cực kỳ minh bạch và tự động hoàn toàn.

## Kiểm thử và kết quả

- **Test hoặc query tôi đã dùng:** Chạy test giao diện trực tiếp bằng lệnh `streamlit run app.py` với các query bất kỳ.
- **Kết quả trước/sau nếu có:** Giao diện hiển thị tốt nội dung chat, khi AI trả lời sẽ có thêm bảng Expand "Xem tài liệu tham khảo" kèm theo điểm độ tin cậy (score).
- **Lỗi đã phát hiện và cách xử lý:** Phát hiện việc chờ code của các thành viên khác sẽ trễ deadline, đã tự xử lý bằng cách bám sát `MODULE_CONTRACTS.md` để tạo `GenerationResult` giả lập.

## Điều còn hạn chế

- **Một hạn chế cụ thể của phần tôi làm:** Code đánh giá evaluation mới chỉ chạy với hàm mock, chưa có số liệu 4 metric thực tế để điền vào `RESULT.md`. UI cũng chưa kịp làm phần bonus highlight chữ.
- **Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện:** Tích hợp ngay hàm `generate_with_citation` thực tế từ Task 10 và code thêm hàm Regex để bôi vàng (highlight) các đoạn text nguồn trong giao diện Streamlit.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- **Ngày:** 25/09/2026
- **Tên thành viên:** Tô Huy Thông
